@model ErrorViewModel
@{
    ViewData["Title"] = "Lỗi";
}

<h1 class="text-danger">Lỗi.</h1>
<h2 class="text-danger">Đã xảy ra lỗi khi xử lý yêu cầu của bạn.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Mã yêu cầu (Request ID):</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Chế độ phát triển (Development)</h3>
<p>
    Bật môi trường <strong>Development</strong> sẽ hiển thị thông tin chi tiết hơn về lỗi đã xảy ra.
</p>
<p>
    <strong>Không nên bật chế độ Development cho ứng dụng đã triển khai.</strong>
    Việc này có thể làm lộ thông tin nhạy cảm từ ngoại lệ tới người dùng cuối.
    Để gỡ lỗi cục bộ, đặt biến môi trường <strong>ASPNETCORE_ENVIRONMENT</strong> thành <strong>Development</strong> và khởi động lại ứng dụng.
</p>
